const display = document.querySelector('.display');const buttons = document.querySelectorAll('.buttons button');let currentInput = '';let operator = null;let previousInput = '';buttons.forEach(button => { button.addEventListener('click', () => { const value = button.textContent; if (button.classList.contains('clear')) { currentInput = ''; operator = null; previousInput = ''; display.value = ''; } else if (button.classList.contains('equals')) { if (currentInput !== '' && previousInput !== '' && operator !== null) { currentInput = eval(previousInput + operator + currentInput); display.value = currentInput; previousInput = ''; operator = null; } } else if (button.classList.contains('operator')) { if (currentInput !== '') { if (previousInput !== '') { previousInput = eval(previousInput + operator + currentInput); } else { previousInput = currentInput; } currentInput = ''; operator = value; display.value = previousInput + operator; } } else { currentInput += value; display.value = currentInput; } });});
